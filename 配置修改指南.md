# Mock数据和网络图片资源配置修改指南

本文档详细说明如何修改Mock数据模拟和网络图片资源的配置。

## 📍 文件位置

### 主要配置文件

1. **DataConfig.java** - 数据配置管理类
   - 路径：`app/src/main/java/com/example/myapplication/DataConfig.java`
   - 功能：配置Mock数据内容和网络图片基础URL

2. **MockDataGenerator.java** - Mock数据生成器
   - 路径：`app/src/main/java/com/example/myapplication/MockDataGenerator.java`
   - 功能：根据配置生成Mock数据

---

## 🔧 修改步骤

### 一、修改Mock数据内容

#### 步骤1：打开DataConfig.java文件

在Android Studio中打开：
```
app/src/main/java/com/example/myapplication/DataConfig.java
```

#### 步骤2：修改标题列表

找到 `MOCK_TITLES` 数组（第16-33行），修改为你想要的标题：

```java
private static final String[] MOCK_TITLES = {
    "你的标题1",
    "你的标题2",
    "你的标题3",
    // 添加更多标题...
    "你的标题N"
};
```

**示例：**
```java
private static final String[] MOCK_TITLES = {
    "北京旅游攻略",
    "上海美食推荐",
    "广州购物指南",
    "深圳科技前沿",
    "成都美食地图"
};
```

#### 步骤3：修改用户名列表

找到 `MOCK_USERNAMES` 数组（第35-52行），修改用户名：

```java
private static final String[] MOCK_USERNAMES = {
    "用户名1",
    "用户名2",
    "用户名3",
    // 添加更多用户名...
};
```

**示例：**
```java
private static final String[] MOCK_USERNAMES = {
    "旅行小达人",
    "美食探索家",
    "购物狂人",
    "科技爱好者",
    "美食博主"
};
```

#### 步骤4：修改内容文本列表

找到 `MOCK_CONTENTS` 数组（第54-70行），修改内容描述：

```java
private static final String[] MOCK_CONTENTS = {
    "你的内容描述1",
    "你的内容描述2",
    "你的内容描述3",
    // 添加更多内容...
};
```

**示例：**
```java
private static final String[] MOCK_CONTENTS = {
    "分享北京最值得去的景点和美食，包含详细的交通和住宿攻略。",
    "上海必吃美食清单，从街头小吃到米其林餐厅，一网打尽！",
    "广州购物攻略，告诉你哪里买最划算，哪里最时尚。",
    "深圳最新科技动态，了解前沿技术和创新产品。",
    "成都美食地图，带你吃遍成都最地道的小吃和火锅。"
};
```

#### 步骤5：修改卡片高度配置

找到 `MOCK_HEIGHTS` 数组（第72行），修改卡片高度（单位：dp）：

```java
private static final int[] MOCK_HEIGHTS = {
    160, 180, 200, 220, 240, 260, 280, 300, 320, 340, 360
};
```

**说明：**
- 数值范围建议：120-400 dp
- 不同高度可以创建瀑布流效果
- 数组长度应该与标题数组长度匹配

---

### 二、修改网络图片URL配置

#### 步骤1：修改图片基础URL

在 `DataConfig.java` 文件中找到第13行：

```java
private static final String IMAGE_BASE_URL = "https://picsum.photos/";
```

**修改为你的图片服务URL：**

**选项1：使用其他占位图片服务**
```java
// Unsplash占位图片服务
private static final String IMAGE_BASE_URL = "https://source.unsplash.com/";

// 或者使用其他服务
private static final String IMAGE_BASE_URL = "https://your-image-service.com/";
```

**选项2：使用自己的图片服务器**
```java
// 你的图片服务器地址
private static final String IMAGE_BASE_URL = "https://api.yourdomain.com/images/";
```

#### 步骤2：修改图片URL生成方法

找到 `getImageUrl` 方法（第81-83行），根据你的图片服务API格式修改：

**当前格式（picsum.photos）：**
```java
public static String getImageUrl(int width, int height, int seed) {
    return IMAGE_BASE_URL + width + "/" + height + "?random=" + seed;
}
```

**如果使用Unsplash：**
```java
public static String getImageUrl(int width, int height, int seed) {
    return IMAGE_BASE_URL + width + "x" + height + "?sig=" + seed;
}
```

**如果使用自己的API：**
```java
public static String getImageUrl(int width, int height, int seed) {
    // 根据你的API格式修改
    return IMAGE_BASE_URL + "get?w=" + width + "&h=" + height + "&id=" + seed;
}
```

#### 步骤3：使用固定图片URL（可选）

如果你想为每个内容项指定固定的图片URL，可以修改 `MockDataGenerator.java`：

**方法1：在DataConfig中添加固定图片URL数组**

在 `DataConfig.java` 中添加：
```java
// 固定图片URL列表（与MOCK_TITLES对应）
private static final String[] MOCK_IMAGE_URLS = {
    "https://example.com/image1.jpg",
    "https://example.com/image2.jpg",
    "https://example.com/image3.jpg",
    // ... 添加更多图片URL
};

public static String[] getMockImageUrls() {
    return MOCK_IMAGE_URLS.clone();
}
```

**方法2：在MockDataGenerator中使用固定URL**

修改 `MockDataGenerator.java` 的 `generateInitialData` 方法：

```java
// 在生成数据时使用固定URL
String[] imageUrls = DataConfig.getMockImageUrls();
String imageUrl = imageUrls[i % imageUrls.length]; // 使用固定URL

// 或者直接指定
String imageUrl = "https://your-image-server.com/image" + i + ".jpg";
```

---

### 三、修改刷新数据内容

#### 步骤1：打开MockDataGenerator.java

找到 `generateRefreshData` 方法（第60-99行）

#### 步骤2：修改刷新数据的标题

```java
String[] refreshTitles = {
    "你的刷新标题1",
    "你的刷新标题2",
    "你的刷新标题3",
    "你的刷新标题4"
};
```

#### 步骤3：修改刷新数据的用户名

```java
String[] refreshUsernames = {
    "你的用户名1",
    "你的用户名2",
    "你的用户名3",
    "你的用户名4"
};
```

---

## 📝 完整示例

### 示例1：修改为旅游主题

**在DataConfig.java中：**

```java
private static final String[] MOCK_TITLES = {
    "北京故宫游览攻略",
    "上海外滩夜景拍摄",
    "广州早茶文化体验",
    "深圳科技馆参观",
    "成都大熊猫基地"
};

private static final String[] MOCK_USERNAMES = {
    "北京导游小王",
    "上海摄影师",
    "广州美食家",
    "深圳科技迷",
    "成都熊猫粉"
};

private static final String[] MOCK_CONTENTS = {
    "详细攻略：如何游览故宫，最佳路线和拍照点推荐。",
    "外滩夜景拍摄技巧，最佳机位和时间分享。",
    "广州早茶文化深度体验，推荐最地道的茶楼。",
    "深圳科技馆最新展览，适合亲子游的好去处。",
    "成都大熊猫基地游览指南，看熊猫的最佳时间。"
};
```

### 示例2：使用自己的图片服务器

**在DataConfig.java中：**

```java
// 修改基础URL
private static final String IMAGE_BASE_URL = "https://cdn.yourdomain.com/images/";

// 修改URL生成方法
public static String getImageUrl(int width, int height, int seed) {
    // 你的API格式：https://cdn.yourdomain.com/images/800x600/12345.jpg
    return IMAGE_BASE_URL + width + "x" + height + "/" + seed + ".jpg";
}
```

### 示例3：使用固定图片URL列表

**在DataConfig.java中添加：**

```java
private static final String[] MOCK_IMAGE_URLS = {
    "https://example.com/images/beijing.jpg",
    "https://example.com/images/shanghai.jpg",
    "https://example.com/images/guangzhou.jpg",
    "https://example.com/images/shenzhen.jpg",
    "https://example.com/images/chengdu.jpg"
};

public static String[] getMockImageUrls() {
    return MOCK_IMAGE_URLS.clone();
}
```

**在MockDataGenerator.java中修改：**

```java
// 在generateInitialData方法中
String[] imageUrls = DataConfig.getMockImageUrls();
String imageUrl = imageUrls[i % imageUrls.length]; // 使用固定URL
```

---

## 🎯 常用图片服务URL格式

### 1. Picsum Photos（当前使用）
```
https://picsum.photos/800/600?random=123
```

### 2. Unsplash Source
```
https://source.unsplash.com/800x600/?nature
https://source.unsplash.com/800x600/?sig=123
```

### 3. Placeholder.com
```
https://via.placeholder.com/800x600
https://via.placeholder.com/800x600/FF0000/FFFFFF?text=Image
```

### 4. Lorem Picsum（与Picsum相同）
```
https://picsum.photos/id/123/800/600
```

---

## ⚠️ 注意事项

1. **数组长度匹配**
   - `MOCK_TITLES`、`MOCK_USERNAMES`、`MOCK_CONTENTS` 数组长度应该相同
   - 如果长度不同，代码会使用取模运算循环使用

2. **图片URL格式**
   - 确保图片URL格式正确
   - 如果使用HTTPS，确保服务器支持SSL证书
   - 测试图片URL是否可访问

3. **图片尺寸**
   - 当前代码根据卡片宽度自动计算图片尺寸
   - 建议使用合适的图片尺寸以节省流量

4. **网络权限**
   - 确保 `AndroidManifest.xml` 中有网络权限：
   ```xml
   <uses-permission android:name="android.permission.INTERNET" />
   ```

5. **图片加载**
   - 当前项目只配置了图片URL，实际加载需要图片加载库（如Glide）
   - 如果图片不显示，需要添加图片加载库

---

## 🔄 修改后如何生效

1. **保存文件**：修改后保存所有文件（Ctrl+S）

2. **同步项目**：点击 Android Studio 的 "Sync Project with Gradle Files" 按钮

3. **重新编译**：点击 "Build" -> "Rebuild Project"

4. **运行应用**：重新运行应用查看效果

---

## 📞 常见问题

**Q: 修改后图片不显示？**
A: 当前项目只配置了URL，需要添加图片加载库（如Glide）才能显示网络图片。

**Q: 如何添加自己的图片？**
A: 可以将图片放在服务器上，然后修改 `IMAGE_BASE_URL` 为你的服务器地址。

**Q: 如何为每个内容指定不同的图片？**
A: 在 `DataConfig.java` 中添加 `MOCK_IMAGE_URLS` 数组，然后在 `MockDataGenerator.java` 中使用。

**Q: 修改后数据没有更新？**
A: 确保重新编译和运行应用，清除应用数据后重新安装。

---

## 📚 相关文件

- `DataConfig.java` - 数据配置
- `MockDataGenerator.java` - 数据生成
- `ContentItem.java` - 数据模型
- `ContentAdapter.java` - 适配器（显示数据）

修改这些文件即可自定义Mock数据和图片资源！

