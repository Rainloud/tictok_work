# GitHub 上传指南

本指南将帮助你将完整的 Android 项目上传到 GitHub 远程仓库。

## 📋 准备工作

### 1. 确保已安装 Git

在命令行中检查 Git 是否已安装：
```bash
git --version
```

如果未安装，请访问 [Git 官网](https://git-scm.com/) 下载安装。

### 2. 创建 GitHub 账号和仓库

1. 访问 [GitHub](https://github.com/)
2. 注册/登录账号
3. 点击右上角 "+" -> "New repository"
4. 填写仓库信息：
   - **Repository name**: `ticktok` (或你喜欢的名字)
   - **Description**: `TikTok风格内容聚合Android应用`
   - **Visibility**: 选择 Public（公开）或 Private（私有）
   - **不要**勾选 "Initialize this repository with a README"（因为本地已有README）
5. 点击 "Create repository"

创建完成后，GitHub 会显示仓库地址，类似：
```
https://github.com/你的用户名/ticktok.git
```

---

## 🚀 上传步骤

### 方法一：使用命令行（推荐）

#### 步骤1：打开终端/命令行

- **Windows**: 在项目文件夹中，按住 `Shift` + 右键，选择"在此处打开 PowerShell 窗口"或"在此处打开命令窗口"
- **Mac/Linux**: 打开终端，使用 `cd` 命令进入项目目录

#### 步骤2：检查 Git 状态

```bash
cd E:\android_devalop\Project\ticktok
git status
```

如果显示 "not a git repository"，需要初始化 Git 仓库。

#### 步骤3：初始化 Git 仓库（如果还没有）

```bash
git init
```

#### 步骤4：检查 .gitignore 文件

确保 `.gitignore` 文件包含以下内容（项目应该已经有了）：

```gitignore
# Built application files
*.apk
*.ap_
*.aab

# Files for the ART/Dalvik VM
*.dex

# Java class files
*.class

# Generated files
bin/
gen/
out/
release/

# Gradle files
.gradle/
build/

# Local configuration file (sdk path, etc)
local.properties

# Proguard folder generated by Eclipse
proguard/

# Log Files
*.log

# Android Studio Navigation editor temp files
.navigation/

# Android Studio captures folder
captures/

# IntelliJ
*.iml
.idea/workspace.xml
.idea/tasks.xml
.idea/gradle.xml
.idea/assetWizardSettings.xml
.idea/dictionaries
.idea/libraries
.idea/jarRepositories.xml
.idea/caches
.idea/modules.xml
.idea/.name
.idea/compiler.xml
.idea/misc.xml
.idea/vcs.xml
*.ipr

# Keystore files
*.jks
*.keystore

# External native build folder generated in Android Studio 2.2 and later
.externalNativeBuild
.cxx/

# Google Services (e.g. APIs or Firebase)
google-services.json

# Freeline
freeline.py
freeline/
freeline_project_description.json

# fastlane
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots
fastlane/test_output
fastlane/readme.md

# Version control
vcs.xml

# lint
lint/intermediates/
lint/generated/
lint/outputs/
lint/tmp/
# lint/reports/
```

#### 步骤5：添加所有文件到 Git

```bash
git add .
```

#### 步骤6：提交文件

```bash
git commit -m "Initial commit: TikTok风格内容聚合应用"
```

#### 步骤7：添加远程仓库

将 `你的用户名` 和 `仓库名` 替换为你的实际信息：

```bash
git remote add origin https://github.com/你的用户名/仓库名.git
```

例如：
```bash
git remote add origin https://github.com/zhangsan/ticktok.git
```

#### 步骤8：推送到 GitHub

```bash
git branch -M main
git push -u origin main
```

如果提示输入用户名和密码：
- **用户名**: 你的 GitHub 用户名
- **密码**: 使用 Personal Access Token（不是 GitHub 密码）

> **注意**: GitHub 从 2021年8月13日起不再支持密码认证，需要使用 Personal Access Token。
> 
> 如何获取 Token：
> 1. GitHub -> Settings -> Developer settings -> Personal access tokens -> Tokens (classic)
> 2. Generate new token (classic)
> 3. 勾选 `repo` 权限
> 4. 生成后复制 Token（只显示一次，请保存好）

---

### 方法二：使用 Android Studio（图形界面）

#### 步骤1：启用版本控制

1. 打开 Android Studio
2. 点击 `File` -> `Settings` (Windows) 或 `Android Studio` -> `Preferences` (Mac)
3. 选择 `Version Control` -> `Git`
4. 确保 Git 路径正确
5. 点击 `OK`

#### 步骤2：初始化 Git 仓库

1. 点击 `VCS` -> `Enable Version Control Integration`
2. 选择 `Git`
3. 点击 `OK`

#### 步骤3：添加文件到 Git

1. 右键点击项目根目录
2. 选择 `Git` -> `Add`
3. 或者使用快捷键 `Ctrl+Alt+A` (Windows) / `Cmd+Option+A` (Mac)

#### 步骤4：提交文件

1. 点击 `VCS` -> `Commit`
2. 在提交信息框中输入：`Initial commit: TikTok风格内容聚合应用`
3. 选择要提交的文件（默认全选）
4. 点击 `Commit`

#### 步骤5：添加远程仓库

1. 点击 `VCS` -> `Git` -> `Remotes...`
2. 点击 `+` 添加远程仓库
3. 填写信息：
   - **Name**: `origin`
   - **URL**: `https://github.com/你的用户名/仓库名.git`
4. 点击 `OK`

#### 步骤6：推送到 GitHub

1. 点击 `VCS` -> `Git` -> `Push`
2. 选择远程仓库 `origin`
3. 点击 `Push`
4. 如果提示登录，输入 GitHub 用户名和 Personal Access Token

---

## 🔍 验证上传

上传完成后，访问你的 GitHub 仓库页面，应该能看到：

- ✅ README.md 文件
- ✅ 配置修改指南.md 文件
- ✅ 所有源代码文件
- ✅ 项目配置文件
- ❌ 不应该看到 `local.properties`（已被 .gitignore 忽略）
- ❌ 不应该看到 `build/` 目录（已被 .gitignore 忽略）

---

## 📝 后续更新代码

### 使用命令行

```bash
# 1. 查看修改的文件
git status

# 2. 添加修改的文件
git add .

# 3. 提交修改
git commit -m "描述你的修改内容"

# 4. 推送到 GitHub
git push
```

### 使用 Android Studio

1. 修改代码后，点击 `VCS` -> `Commit`
2. 输入提交信息
3. 点击 `Commit and Push`

---

## ⚠️ 常见问题

### 问题1：提示 "remote origin already exists"

**解决方法**：
```bash
# 删除现有的远程仓库
git remote remove origin

# 重新添加
git remote add origin https://github.com/你的用户名/仓库名.git
```

### 问题2：推送时提示认证失败

**解决方法**：
1. 确保使用 Personal Access Token 而不是密码
2. 或者使用 SSH 方式：
   ```bash
   # 生成 SSH 密钥（如果还没有）
   ssh-keygen -t ed25519 -C "your_email@example.com"
   
   # 添加 SSH 密钥到 GitHub
   # 然后使用 SSH URL
   git remote set-url origin git@github.com:你的用户名/仓库名.git
   ```

### 问题3：提示 "failed to push some refs"

**解决方法**：
```bash
# 先拉取远程代码
git pull origin main --allow-unrelated-histories

# 解决冲突后再次推送
git push
```

### 问题4：想忽略某些文件但忘记添加到 .gitignore

**解决方法**：
```bash
# 从 Git 中移除文件（但保留本地文件）
git rm --cached 文件名

# 添加到 .gitignore
# 然后提交
git add .gitignore
git commit -m "Update .gitignore"
git push
```

---

## 🔐 安全建议

1. **不要上传敏感信息**：
   - ✅ `local.properties` 已在 .gitignore 中（包含 SDK 路径）
   - ✅ 不要上传密钥文件（.jks, .keystore）
   - ✅ 不要上传 API 密钥

2. **检查 .gitignore**：
   确保以下文件/目录被忽略：
   - `local.properties`
   - `build/`
   - `*.apk`
   - `.idea/`（部分文件）

3. **使用 Private 仓库**：
   如果项目包含敏感信息，使用 Private 仓库。

---

## 📚 相关资源

- [Git 官方文档](https://git-scm.com/doc)
- [GitHub 帮助文档](https://docs.github.com/)
- [创建 Personal Access Token](https://docs.github.com/en/authentication/keeping-your-account-and-data-secure/creating-a-personal-access-token)

---

## ✅ 检查清单

上传前请确认：

- [ ] 已创建 GitHub 仓库
- [ ] 已检查 .gitignore 文件
- [ ] 已确认 local.properties 被忽略
- [ ] 已准备好 Personal Access Token
- [ ] 已提交所有需要的文件
- [ ] 已添加远程仓库地址
- [ ] 已成功推送到 GitHub
- [ ] 已在 GitHub 上验证文件完整性

---

**完成！** 🎉 你的项目现在已经上传到 GitHub 了！

